import { Canvas } from "@modules/graphics";
import { HomeScreenProps as Props } from "./Home.types";

function Home({}: Props) {
    return (
        <div>
            
            <Canvas />
        </div>
    );
}

export default Home;
